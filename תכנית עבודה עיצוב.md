# תכנית עבודה: עיצוב

מסמך זה מתאר איך מעבירים את המערכת הקיימת לשפה החזותית **"פטינה"** שנבנתה במוקאפ
(`patina-crm-mockup.html`, ומפורסם גם כדף חי).

**אין כאן שום פיצ׳ר חדש.** אותם מסכים, אותם נתונים, אותה התנהגות — רק היררכיה,
צבע, טיפוגרפיה ומרווחים אחרים. כל שלב שמוסיף התנהגות מסומן במפורש כ"רשות".

מסמך זה הוא אח ל-`BUILD-PLAN.md`, לא תחליף לו. `BUILD-PLAN.md` נשאר מקור האמת
לפיצ׳רים; מסמך זה הוא מקור האמת לעיצוב.

---

## סטטוס ביצוע (עודכן 5.9.2026)

**כל השלבים הושלמו ונמצאים בפרודקשן.**

| שלב | מצב | קומיט |
|---|---|---|
| 0. יסודות — צבע וגופנים | ✅ בפרודקשן | `fcc9dc6` |
| 1. מסגרת — סרגל צד | ✅ בפרודקשן | `cc72940` |
| 2. רכיבים + מד חלון | ✅ בפרודקשן | `7dfe314` |
| 3א. דף הבית | ✅ בפרודקשן | `9cb7699` |
| 3ב. מי שיצר קשר | ✅ בפרודקשן | `249f3be` |
| 3ג. כרטיס לקוח | ✅ בפרודקשן | `ddab3c1` |
| 3ד. אנשי קשר | ✅ בפרודקשן | `b7240d9` |
| 3ה. פגישות, אירועים, קורסים | ✅ בפרודקשן | `d1e3b95` |
| 3ו. וואטסאפ, ניוזלטר, הגדרות | ✅ בפרודקשן | `81bb0b0` |
| 4. לוח המסע | ✅ בפרודקשן | `079adbb` |
| 5. ליטוש ומצב כהה | ✅ בפרודקשן | `53fca07` |

**מה שנבדק בכל שלב:** `npx tsc --noEmit` נקי, `npm run lint` נקי,
`next build` ללא אזהרות, ואימות מול ה-CSS שהשרת מגיש בפועל.

**איך אומתה הפריסה בלי התחברות.** כל המסכים מאחורי התחברות, וקלוד אינו מזין
סיסמאות. במקום זה נבנתה רתמת רינדור (`react-dom/server` + ה-CSS החי) שמרנדרת
את הרכיבים האמיתיים לעמוד עצמאי. היא מדדה גלישה אופקית, רוחבי עמודות, אורכי
פסים, ויחסי ניגודיות — ותפסה בין השאר שחשד לגבי צפיפות כרטיס השאלון היה שגוי.
**לא נגעו ב-`proxy.ts` ולא ברשימת הנתיבים הציבוריים.**

**מצב כהה — נדלק.** מתג בתחתית סרגל הצד, שלושה מצבים (בהיר / לפי המערכת /
כהה), עם סקריפט חוסם ב-`<head>` שמונע הבהוב. מעבר ניגודיות אוטומטי מול WCAG AA
העלה שלושה תיקונים, וכולם נסגרו.

**מה לא נעשה, במכוון:**
- שבבי סינון וחיפוש ב"מי שיצר קשר", ולשוניות בתיבת התשובה — התנהגות חדשה.
- "הערה מהירה", "שליחת הודעה", "קביעת פגישה", "הכנסה למסע" — אין להם פעולה.
- `google-meet-badge` נשאר לבן: הוא משמש רק בדף ההזמנה הציבורי, והצבעים שם
  הם של גוגל.
- תשעת הדפים הציבוריים — מחוץ לתוכנית, כפי שהוגדר מלכתחילה.

---|---|---|---|
| 0. יסודות — צבע וגופנים | ✅ **בפרודקשן** | `fcc9dc6` | מוזג ל-`main` |
| 1. מסגרת — סרגל צד | 🟡 מוכן, לא נדחף | `cc72940` | `design/stage-1-sidebar` |
| 2. רכיבים + מד חלון | 🟡 מוכן, לא נדחף | `7dfe314` | `design/stage-2-components` |
| 3. תשעה מסכים | ⬜ טרם התחיל | — | — |
| 4. לוח המסע | ⬜ טרם התחיל | — | — |
| 5. ליטוש ומצב כהה | ⬜ טרם התחיל | — | — |

**הענפים מוערמים:** `design/stage-2-components` מכיל את 0+1+2. מיזוג שלו ל-`main`
מעלה את שלושתם יחד.

**מה נבדק בכל שלב:** `npx tsc --noEmit` נקי, `npm run lint` נקי. שלב 0 אומת גם
מול הפרודקשן החי (אסימונים וגופנים). שלב 1 אומת בצילום מסך מהמערכת המקומית —
הסרגל, לשוניות המשנה, כרטיס הערוץ ופירורי הלחם עובדים. **בדיקה חסרה:** המגירה
במובייל לא נבדקה על ידי אף אחד.

**מה נדחה במכוון בשלב 1, ולמה:**
- *מונים בסרגל* (7 ליד "לקוחות") — דורשים שלוש שאילתות בכל טעינת עמוד במערכת.
- *כפתור חיפוש* — חיפוש גלובלי לא קיים; כפתור מת גרוע מכפתור חסר.

**ממצא מהשטח:** כרטיס הערוץ בסרגל הראה שהשליחה האוטומטית **מושהית** — הקרון
אינו שולח דבר. זה בדיוק מה שהכרטיס נועד לחשוף. יש להחליט אם להדליק.

**הצעד הבא:** מיזוג `design/stage-2-components` ל-`main`, ואז שלב 3א (דף הבית).

---

## תיקון: מה שכבר קיים במסע הלקוח

בשיחה קודמת אמרתי שכדי להפוך את מסע הלקוח ללוח צריך שלושה דברים חדשים בפיתוח.
**בדקתי בקוד — שלושתם כבר קיימים.** הנה מה שיש היום:

| מה שאמרתי שחסר | מה שקיים בפועל |
|---|---|
| מיקום X/Y לכל כרטיסייה | `journey_steps.pos_x`, `pos_y` — מיגרציה `0019_journey_graph.sql` |
| חיבורים כרשומה נפרדת | טבלת `journey_edges` עם `from_step_id`, `to_step_id`, `priority` |
| שלב מסוג תנאי | תנאי יושב **על הקשת** ולא על הצומת: `condition in ('always','if_replied','if_not_replied')` |
| ממשק לוח | `src/app/(dashboard)/journeys/[id]/canvas.tsx` — 596 שורות של לוח עובד |

הלוח הקיים כבר יודע: כרטיסיות נגררות עם שמירת מיקום לשרת, חצי SVG עם ראשי חץ,
יצירת ומחיקת חיבורים, עריכת תנאי על כל חץ, פאנל שמראה "מי מגיע לכאן" ו"לאן
ממשיכים", וכרטיסיית טיוטה לפני שמירה.

**המשמעות:** שלב 4 בתוכנית הזו הוא **עיצוב מחדש של לוח קיים**, לא בנייה של לוח
חדש. אין מיגרציה, אין שינוי במנוע האוטומציה, ואין סיכון לנתונים.

הפרש אחד שכן קיים בין המוקאפ לקוד: במוקאפ מחברים חץ **בגרירה** מהעיגול, ובקוד
מחברים **בשתי לחיצות** — לוחצים על נקודת החיבור, ואז על כרטיסייה. זו לא הזנחה;
יש בקוד הערה מפורשת שמסבירה שגרירה כמעט בלתי אפשרית במסך מגע. זו החלטה שצריכה
להתקבל, לא באג שצריך לתקן — ראו "החלטות" בסוף.

---

## מצב הקוד היום — במה לא להתנגש

נכון ל-4.9.2026, לפי `BUILD-PLAN.md`:

- **שלב 6 (מטא וגרואו)** — הקוד עלה בקומיט `78c8480`, אבל **מיגרציה 0030 טרם רצה**
  וההפעלה מול מטא וגרואו טרם נעשתה. מסך `/settings/meta-forms` נולד עכשיו ויעוצב
  יחד עם שאר ההגדרות בשלב 3ו.
- **שלב 3 (ניוזלטר)** — חסום על אישור חשבון Postmark. אף מייל לא יצא מעולם.
  אין קשר לעיצוב, אבל אסור להסיק ממסך הניוזלטר שהוא נבדק מקצה לקצה.
- **משתני סביבה** שטרם הוגדרו בפרודקשן: `META_LEADS_VERIFY_TOKEN`,
  `GROW_WEBHOOK_SECRET`.

**כלל התנגשות:** שלב 0 בתוכנית הזו נוגע רק ב-`src/app/globals.css` וב-
`src/app/layout.tsx`. אף אחד מהם אינו חלק מהעבודה הפתוחה, ולכן **אפשר להתחיל
בשלב 0 מיד** בלי לחכות לסגירת מטא וגרואו.

---

## כללים מחייבים לכל השלבים

1. **`BUILD-PLAN.md` אומר "אל תמציא מערכת עיצוב חדשה".** התוכנית הזו מחליפה את
   השורה הזו — אבל רק עבורה. בסוף שלב 2, אותו כלל מצביע על המערכת **החדשה**,
   וכל פיצ׳ר עתידי חייב להשתמש בה.
2. **שלב אחד = קומיט אחד.** כל שלב עומד בפני עצמו וניתן לביטול בנפרד
   (`git revert`) בלי לגעת בשאר.
3. **לא נוגעים ב-URL-ים, בשמות טבלאות או במבנה תיקיות.** עיצוב בלבד.
4. **לא נוגעים ב:** `src/app/login`, `src/app/book`, `src/app/course`,
   `src/app/event`, `src/app/api/`, `scripts/`. הדפים הציבוריים מעוצבים בשלב
   נפרד ורק אם תבקש.
5. **בדיקות סוף שלב, זהות לכולם:** `npx tsc --noEmit` נקי, `npm run lint` נקי,
   ומעבר ידני על כל מסך שהשלב נגע בו — במצב בהיר ובמצב כהה.
6. **כל טקסט בממשק בעברית, לשון זכר, RTL.**

---

## שלב 0 · יסודות

**מה עושים.** מחליפים את משתני ה-CSS ב-`globals.css` בפלטת פטינה: נייר חמים,
דיו ירקרק, ורדיגריס כמבטא, ושש קבוצות הניווט מכוילות לבהירות אחידה. מוסיפים
בלוק מקביל למצב כהה. מוסיפים שתי גופנים ל-`layout.tsx`: IBM Plex Sans Hebrew
לגוף הממשק ו-IBM Plex Mono לנתוני LTR (טלפונים, תאריכים, slugs). Rubik נשאר
לכותרות ולמספרים.

**קבצים.** `src/app/globals.css`, `src/app/layout.tsx`

**למה קודם.** כל מסך במערכת כבר משתמש ב-`var(--primary)`, `.card`,
`.btn-primary` וכו׳. ברגע שהמשתנים מתחלפים, **כל המערכת מתחלפת בבת אחת** בלי
לגעת בשום עמוד. זה השלב עם היחס הכי טוב בין מאמץ לתוצאה.

**מה רואים בסוף.** כל המערכת בצבעים ובגופנים החדשים. הפריסה עדיין ישנה.

**זמן.** מפגש אחד · **סיכון.** נמוך — קובץ אחד, ביטול מיידי.

---

## שלב 1 · מסגרת הניווט

**מה עושים.** מחליפים את שתי שורות הניווט העליונות בסרגל צד קבוע מימין, עם
לשוניות משנה שנפתחות מתחת לקבוצה הפעילה, מוני פריטים, כרטיס מצב הערוץ בתחתית,
וסרגל עליון עם פירורי לחם וחיפוש.

**קבצים.** `src/components/dashboard-nav.tsx` (כתיבה מחדש),
`src/app/(dashboard)/layout.tsx`

**שים לב.** הרכיב הקיים כבר מחזיק את כל מבנה שש הקבוצות ואת לוגיקת
`isTabActive` — הלוגיקה נשמרת, רק התצוגה משתנה. במסך צר הסרגל מתקפל לסרגל
עליון; אין מצב שבו הניווט נעלם.

**מה רואים בסוף.** כל המערכת בשלד החדש. תוכן העמודים עדיין ישן.

**זמן.** מפגש אחד · **סיכון.** בינוני — הניווט נמצא בכל עמוד, אז באג נראה בכל
מקום. מנגד זה רכיב אחד, וה-revert מחזיר הכול.

---

## שלב 2 · שכבת הרכיבים

**מה עושים.** משדרגים את הפרימיטיבים המשותפים: כפתורים בשלוש דרגות, תוויות
סטטוס עם נקודה, כרטיסים בשלוש רמות גובה, טבלאות, שדות קלט, וריבוע האייקון
הצבעוני (`glyph`) שחוזר בכל כותרת מקטע.

**רכיב חדש אחד: מד חלון 24 השעות.** פס קטן שמראה כמה זמן נשאר לענות בטקסט
חופשי. הלוגיקה כבר קיימת ב-`isWithin24HourWindow` ו-`windowRemainingMs`
(`src/lib/whatsapp-cloud.ts`) — זו עטיפה חזותית בלבד, שתשמש בשלושה מסכים.

**קבצים.** `src/app/globals.css` (שכבת `@layer components`),
`src/components/status-badge.tsx`, `src/components/status-picker.tsx`,
`src/lib/status-colors.ts`, **חדש:** `src/components/window-meter.tsx`

**מלכודת אחת.** `status-colors.ts` מחזיק מחרוזות Tailwind שלמות בכוונה — Tailwind v4
סורק את המקור ולא מייצר מחלקות שנבנות בזמן ריצה. בנוסף, אותם 12 שמות צבע אכופים
ב-`check constraint` במסד (`0003_statuses.sql`). לכן: **משנים את המראה, לא את
השמות.** `blue` נשאר `blue` — הוא פשוט נראה אחרת.

**זמן.** מפגש אחד עד שניים · **סיכון.** נמוך-בינוני.

---

## שלב 3 · מסכים, לפי סדר השימוש היומי

כל תת-שלב עומד בפני עצמו וניתן לעצירה באמצע. הסדר לפי כמה פעמים ביום אתה נכנס
למסך.

**3א · דף הבית** — ארבעה מדדים עם גרפי מגמה זעירים, ציר זמן ל"היום" עם קו
"עכשיו", ורשימת "דורש טיפול" מקובצת.
קבצים: `src/app/(dashboard)/page.tsx`, **חדש:** `src/components/metric-tile.tsx`,
`src/components/day-timeline.tsx`

**3ב · מי שיצר קשר** — שורות בסגנון רשימת שיחות, מד החלון בכל שורה, סימון עדין
למי שטרם נענה.
קבצים: `src/app/(dashboard)/active/active-list.tsx`, `active/contact-row.tsx`

**3ג · כרטיס לקוח** — היפוך הפריסה: השיחה היא העמודה הרחבה, הפרטים לצידה. בועות
שיחה, שורות מערכת, ותיבת תשובה שאומרת במפורש מה מותר עכשיו.
קבצים: `src/app/(dashboard)/contacts/[id]/page.tsx`,
`src/components/conversation.tsx`, `src/components/reply-box.tsx`

**3ד · אנשי קשר** — טבלה בצפיפות נכונה, נתוני LTR באות מונו, סרגל פעולות מרוכזות
שנפתח בסימון שורה.
קבצים: `src/app/(dashboard)/contacts/contacts-table.tsx`, `contacts/page.tsx`

**3ה · פגישות, אירועים, קורסים** — כרטיסי סוגי פגישות עם ריבוע הזמן, רשת זמינות
שבועית, פס תפוסה ומשפך הרשמה.
קבצים: `src/app/(dashboard)/booking/`, `events/`, `courses/`

**3ו · וואטסאפ, ניוזלטר, הגדרות** — טבעת מכסה יומית, שני כרטיסי חלון 24 השעות
זה לצד זה, עורך ניוזלטר עם תצוגה מקדימה צמודה, והגדרות בארבע לשוניות (כולל
`meta-forms` שנולד בשלב 6 של `BUILD-PLAN.md`).
קבצים: `src/app/(dashboard)/whatsapp/page.tsx`, `newsletter/`, `settings/`

**זמן.** מפגש לכל תת-שלב, בערך שישה · **סיכון.** נמוך — כל מסך מבודד.

---

## שלב 4 · לוח המסע

**מה עושים.** מעצבים מחדש את `canvas.tsx` הקיים: כרטיסיות בשפה החדשה עם ריבוע
ערוץ צבעוני ותווית תזמון, רקע נקודות, חצים בעקומת בזייה עם ראשי חץ צבעוניים,
ו-× לניתוק בריחוף. מוסיפים כפתור "סידור אוטומטי" שמחזיר את הפריסה לברירת מחדל.

**קבצים.** `src/app/(dashboard)/journeys/[id]/canvas.tsx`,
`journeys/[id]/page.tsx`

**רשות — לא חלק מהעיצוב.** חיבור בגרירה בנוסף לחיבור בשתי לחיצות. הגרירה
נעימה יותר בעכבר, שתי הלחיצות עובדות במגע. אפשר לתמוך בשתיהן, אבל זו תוספת
התנהגות ולכן החלטה נפרדת.

**מה אין כאן.** אין מיגרציה, אין שינוי סכימה, אין נגיעה במנוע האוטומציה.

**זמן.** מפגש אחד · **סיכון.** נמוך.

---

## שלב 5 · ליטוש

**מה עושים.** מעבר על כל מסך במצב כהה ותיקון ניגודיות; מעבר נגישות — טבעת פוקוס
נראית בכל פקד, ניווט מקלדת מלא, `aria` נכון בטבלאות ובלשוניות; מעבר במסך צר.

**קבצים.** נגיעות קטנות בכל מקום.

**זמן.** מפגש אחד · **סיכון.** נמוך.

---

## סיכום

| שלב | מה משתנה | קבצים עיקריים | זמן | סיכון |
|---|---|---|---|---|
| 0 | צבע וגופנים בכל המערכת | `globals.css`, `layout.tsx` | מפגש | נמוך |
| 1 | סרגל צד במקום ניווט עליון | `dashboard-nav.tsx` | מפגש | בינוני |
| 2 | כפתורים, תוויות, טבלאות, מד חלון | `globals.css`, 4 רכיבים | 1–2 מפגשים | נמוך-בינוני |
| 3 | תשעה מסכים, לפי סדר השימוש | לפי מסך | ~6 מפגשים | נמוך |
| 4 | עיצוב מחדש ללוח המסע הקיים | `canvas.tsx` | מפגש | נמוך |
| 5 | מצב כהה, נגישות, מסך צר | הכול | מפגש | נמוך |

**סך הכול: 11–12 מפגשים.** אחרי שלב 1 המערכת כבר נראית חדשה לגמרי, גם אם
שלב 3 עוד לא התחיל.

---

## שלוש החלטות שצריך ממך

**1. מצב כהה — בפנים או בחוץ?**
המוקאפ תומך בשניהם. במערכת האמיתית זה מוסיף מעבר על כל מסך בשלב 5, ומחייב כל
פיצ׳ר עתידי להיבדק פעמיים. *המלצה:* להכניס. העלות היא בעיקר בשלב 0, ושאר
השלבים כמעט לא מרגישים אותה.

**2. חיבור חצים בלוח המסע — גרירה, לחיצות, או שניהם?**
הקוד בחר בשתי לחיצות בכוונה, בגלל מסכי מגע. *המלצה:* לתמוך בשתיהן — גרירה
בעכבר, לחיצות במגע. זו תוספת קטנה, אבל היא תוספת התנהגות ולא עיצוב.

**3. סדר — מסגרת קודם, או מסך קודם?**
*המלצה:* מסגרת קודם (0→1→2). כל המערכת מתחלפת תוך שני מפגשים, ואז כל מסך
משתפר בנפרד בלי דחיפות. החלופה — מסך אחד שלם בעיצוב חדש ליד מסכים ישנים — נראית
שבורה יותר, לא פחות.

---

## מה לא בתוכנית הזו

- **דפים ציבוריים** (`/book`, `/event`, `/course`, `/login`) — הלקוחות רואים
  אותם, לא אתה. עיצוב נפרד, אחרי שהמערכת הפנימית סגורה.
- **פיצ׳רים חדשים** מכל סוג. אם משהו במוקאפ נראה כמו יכולת חדשה — הוא תצוגה של
  נתון שכבר קיים.
- **שינויים במנוע האוטומציה, בשליחה, או בסכימת המסד.** אפס מיגרציות.
- **מובייל אמיתי** (לא רק "לא נשבר במסך צר"). אם תרצה אפליקציה בטלפון — זו
  תוכנית אחרת.
