# המשך עבודה: עיצוב פטינה

**מסמך מסירה.** נכתב בסוף מפגש 4.9.2026 כדי שמפגש חדש ימשיך מכאן בלי לחקור מחדש.

> **לקלוד בשיחה חדשה:** קרא את המסמך הזה במלואו לפני שאתה נוגע בקוד, ואז את
> `תכנית עבודה עיצוב.md` (התוכנית המלאה) ואת `AGENTS.md`. **הקטע "מלכודות" למטה
> הוא החשוב ביותר** — כל פריט שם עלה בטעות אמיתית או בגילוי בקוד, ולא תמצא אותו
> בשום מקום אחר.

---

## 1. מה זה הפרויקט

CRM בעברית (RTL) לעסק של גיא זורי — תוכנית שחרור חסמים בת 6 שבועות. Next.js 16
עם Turbopack, Tailwind v4, Supabase, פריסה ל-Vercel.

**"פטינה"** היא השפה החזותית החדשה: נייר חמים, דיו ירקרק, ורדיגריס כמבטא יחיד.
היא נבנתה קודם כמוקאפ מלא, ואז מועברת לקוד שלב-שלב.

- **המוקאפ:** `patina-crm-mockup.html` בשורש, ומפורסם ב-
  https://claude.ai/code/artifact/598c35d7-afc2-4ddf-93a9-71428204148a
- **התוכנית:** `תכנית עבודה עיצוב.md`, ומפורסמת ב-
  https://claude.ai/code/artifact/ebe9ae55-5a34-4770-8ab5-97291661cc50

---

## 2. איפה הדברים עומדים

`main` = `7dfe314`, **בפרודקשן ומאומת**. אין הפרש מול `origin/main`.

| שלב | מצב | קומיט |
|---|---|---|
| 0. יסודות — צבע, גופנים, מצב כהה | ✅ בפרודקשן | `fcc9dc6` |
| 1. מסגרת — סרגל צד + פירורי לחם | ✅ בפרודקשן | `cc72940` |
| 2. רכיבים משותפים + מד חלון 24 השעות | ✅ בפרודקשן | `7dfe314` |
| **3. פריסת המסכים** | ⬜ **הצעד הבא** | — |
| 4. לוח המסע | ⬜ | — |
| 5. ליטוש, נגישות, הדלקת מצב כהה | ⬜ | — |

ענפי הביניים (`design/stage-0-tokens`, `design/stage-1-sidebar`,
`design/stage-2-components`) מוזגו כולם ואפשר למחוק אותם.

### מה שונה בקוד עד כה

```
src/app/globals.css                  שוכתב — אסימונים, מצב כהה, שכבת רכיבים
src/app/layout.tsx                   שלושה גופנים במקום אחד
src/app/(dashboard)/layout.tsx       קורא מצב ערוץ ועוטף ב-DashboardShell
src/components/dashboard-shell.tsx   חדש — סרגל צד, מגירה, סרגל עליון
src/components/dashboard-nav.tsx     נמחק — הוחלף ב-shell
src/components/window-meter.tsx      חדש — מד חלון 24 השעות
src/components/status-badge.tsx      נקודה בצבע הסטטוס
src/components/status-picker.tsx     תפריט עבר לאסימוני צבע
src/components/reply-box.tsx         משתמש ב-WindowMeter
src/lib/status-colors.ts             וריאנטים כהים; שמות הצבעים לא השתנו
```

---

## 3. הצעד הבא: שלב 3

פריסת המסכים, לפי סדר השימוש היומי. **30 מסכים בסך הכול** מאחורי התחברות, אבל
רובם חולקים רכיבים ומתעצבים בקבוצות:

| תת-שלב | מה | כמה מסכים |
|---|---|---|
| **3א** | **דף הבית** — ארבעה מדדים, ציר "היום" עם קו עכשיו, "דורש טיפול" | 1 |
| 3ב | מי שיצר קשר — שורות שיחה + מד חלון בכל שורה | 2 |
| 3ג | כרטיס לקוח — השיחה כעמודה רחבה, פרטים לצידה | 1 |
| 3ד | אנשי קשר — טבלה, סרגל פעולות מרוכזות | 1 |
| 3ה | פגישות, אירועים, קורסים | 12 |
| 3ו | וואטסאפ, ניוזלטר, הגדרות | 9 |
| 4 | לוח המסע | 2 |
| — | כללים, תבניות | 2 |

**התחל ב-3א.** הפריסה המדויקת נמצאת במוקאפ, מסך "בית".

---

## 4. מלכודות — קרא את זה

### 4.1 RTL: `start` הוא ימין, `end` הוא שמאל

הטעות שעשיתי ארבע פעמים בקובץ אחד. ב-`dir="rtl"`:

| Tailwind | מה זה בפועל |
|---|---|
| `border-s`, `start-0`, `ms-*`, `ps-*` | **ימין** |
| `border-e`, `end-0`, `me-*`, `pe-*` | **שמאל** |

סרגל שיושב בימין המסך מקבל `border-e` (הקו המפריד פונה שמאלה, אל התוכן).
מגירה שנפתחת מימין מקבלת `start-0`. **בדוק כל מחלקה לוגית פעמיים.**

### 4.2 לוח המסע כבר קיים — אל תבנה אותו מחדש

`src/app/(dashboard)/journeys/[id]/canvas.tsx` הוא 596 שורות של לוח עובד:
כרטיסיות נגררות עם שמירת מיקום לשרת, חצי SVG, יצירת ומחיקת חיבורים, ועריכת
תנאי על כל חץ. במסד כבר יש `journey_steps.pos_x/pos_y` וטבלת `journey_edges`
עם תנאי על הקשת (`always` / `if_replied` / `if_not_replied`) — מיגרציה
`0019_journey_graph.sql`.

**שלב 4 הוא עיצוב מחדש בלבד. אפס מיגרציות, אפס נגיעה במנוע האוטומציה.**

הפרש אחד מהמוקאפ: בקוד מחברים חץ בשתי לחיצות, במוקאפ בגרירה. יש בקוד הערה
שמסבירה שגרירה כמעט בלתי אפשרית במסך מגע. גיא אישר לתמוך בשתיהן — זו תוספת
התנהגות, לא עיצוב.

### 4.3 `dark:` מקשיב ל-`data-theme`, לא למערכת ההפעלה

מוגדר בראש `globals.css`:

```css
@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));
```

המצב הכהה **מוגדר במלואו אבל כבוי**, ונדלק רק כש-`data-theme="dark"` יושב על
`<html>`. הסיבה: מסכים רבים עוד מחזיקים `bg-white` ישירות ב-JSX, והדלקה
אוטומטית תיתן כרטיסים לבנים על רקע שחור. **כל מסך שמעוצב בשלב 3 חייב להחליף
`bg-white` ל-`bg-[var(--surface)]`.** המתג נדלק רק בשלב 5, אחרי שכולם הוחלפו.

לבדיקה: `document.documentElement.setAttribute('data-theme','dark')` בקונסול.

### 4.4 `status-colors.ts` — שנים-עשר שמות נעולים

המחרוזות חייבות להישאר ליטרליות בקוד (Tailwind v4 סורק את המקור ולא מייצר
מחלקות שנבנות בזמן ריצה), ואותם 12 שמות אכופים ב-`check constraint` במסד
(`0003_statuses.sql`). **משנים את המראה, לא את השמות.**

### 4.5 שרת פיתוח: יש שיחה שנייה שרצה באותה תיקייה

- Next.js מסרב להריץ שני שרתי dev על אותה תיקייה. `autoPort: true` כבר נוסף
  ל-`.claude/launch.json`, אבל זה לא פותר את הסירוב.
- **אל תריץ `npm run build` בתיקייה** כששרת של שיחה אחרת חי — הם חולקים `.next/`.
- לבדיקה ויזואלית: נווט ל-`http://localhost:3000` הקיים במקום להרים שרת חדש.

### 4.6 אי אפשר לאמת מסכים ויזואלית — גיא הוא הבודק

כל 30 המסכים מאחורי התחברות, וקלוד לא מזין סיסמאות. מה שכן אפשר:
`npx tsc --noEmit`, `npm run lint`, בדיקת ה-CSS בפרודקשן, וקריאת
`.next/dev/logs/next-development.log`. **את הפריסה עצמה גיא חייב לראות ולצלם.**

### 4.7 דחיפה בלי להפריע לשיחה השנייה

`git checkout main && git merge` מחזיר קבצים לישן ואז מחיל מחדש — שתי
קומפילציות מיותרות בשרת של השיחה השנייה. במקום זה:

```bash
git branch -f main <ענף>   # מקדם את main בלי לגעת בקבצים
git checkout main          # עץ הקבצים כבר זהה — אפס כתיבות
git push origin main
```

אמת עם `git rev-parse HEAD^{tree}` לפני ואחרי — אותה טביעת אצבע.

### 4.8 אימות פרודקשן

הדומיין הציבורי הוא `crm-system-eight-omega.vercel.app` בלבד; האחרים חסומים
ב-Vercel SSO ומחזירים 302 על כל בקשה, כך שבדיקה מולם תמיד "עוברת".

ה-CSS יושב ב-`/_next/static/immutable/chunks/*.css` (Turbopack) — **לא**
ב-`/_next/static/css/`. הטעות הזו גרמה לי לדווח בטעות שדיפלוי תקין נכשל.
המזעור גם מוריד מרכאות: חפש `data-theme=dark`, לא `data-theme="dark"`.

### 4.9 `BUILD-PLAN.md` אומר "אל תמציא מערכת עיצוב חדשה"

הכלל הזה **הוחלף** לצורך עבודת העיצוב. מסוף שלב 2, הוא מצביע על המערכת החדשה,
וכל פיצ׳ר עתידי חייב להשתמש בה.

### 4.10 שגיאות Server Action נבלעות בפרודקשן

`throw` בעברית מתורגם אצל המשתמש ל-React #441. הדפוס שמחליף אותו כבר קיים
בקוד — חפש `ActionForm` ו-`ReplyResult`.

---

## 5. החלטות פתוחות שצריכות את גיא

1. **השליחה האוטומטית מושהית.** כרטיס הערוץ בסרגל מראה את זה באדום — הקרון לא
   שולח כלום: לא תזכורות פגישה ולא מסעות. המתג ב-**הגדרות ← בלמי שליחה**.
   *לא נגעתי:* הדלקה שולחת הודעות אמיתיות ללקוחות.

2. **9 דפים ציבוריים מחוץ לתוכנית** — `/book/[slug]`, `/event/[slug]`,
   `/course/[slug]`, `/login` ועמודי תודה והטמעה. הם קיבלו צבעים וגופנים משלב 0
   אבל לא פריסה. **הלקוחות רואים אותם, לא גיא** — ואפשר לטעון שדף ההרשמה לסדנה
   חשוב יותר ממסך פנימי. השאלה הועלתה ולא נענתה.

3. **המגירה במובייל לא נבדקה** על ידי אף אחד.

4. **נדחו במכוון בשלב 1:** מונים בסרגל (דורשים שלוש שאילתות בכל טעינת עמוד),
   וכפתור חיפוש (חיפוש גלובלי לא קיים; כפתור מת גרוע מכפתור חסר).

---

## 6. חובות מחוץ לעיצוב (מ-`BUILD-PLAN.md`)

לא קשור לעיצוב, אבל פתוח ועלול להתנגש:

- **מיגרציה `0030_webhook_inbox.sql` טרם רצה.**
- **מטא וגרואו** — הקוד עלה (`78c8480`), ההפעלה מול מטא וגרואו טרם נעשתה.
  משתני סביבה חסרים בפרודקשן: `META_LEADS_VERIFY_TOKEN`, `GROW_WEBHOOK_SECRET`.
- **ניוזלטר** — חסום על אישור חשבון Postmark. **אף מייל לא יצא מהמערכת מעולם.**

---

## 7. כללי עבודה שהוסכמו

- **שלב אחד = קומיט אחד**, על ענף נפרד, וניתן לביטול בנפרד.
- **בדיקות סוף שלב:** `npx tsc --noEmit` נקי, `npm run lint` נקי, ומעבר ידני של
  גיא על כל מסך שהשלב נגע בו.
- **לא נוגעים** ב-`src/app/login`, `src/app/book`, `src/app/api/`, `scripts/`.
- **לא משנים** נתיבי URL, שמות טבלאות או מבנה תיקיות.
- **כל טקסט בממשק בעברית, לשון זכר, RTL.**
- גיא הוא בעל העסק ולא מפתח: **מסך אחד בכל פעם**, הסבר בעברית פשוטה, ובלי
  לבקש ממנו להריץ פקודות.
