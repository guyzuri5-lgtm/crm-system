# המשך עבודה: עיצוב פטינה

**מסמך מסירה.** עודכן 5.9.2026, בסוף המפגש שסגר את כל אחד-עשר השלבים.

> **לקלוד בשיחה חדשה:** קרא את המסמך הזה במלואו לפני שאתה נוגע בקוד, ואז את
> `תכנית עבודה עיצוב.md` ואת `AGENTS.md`. **הקטע "מלכודות" הוא החשוב ביותר** —
> כל פריט שם עלה בטעות אמיתית או בגילוי בקוד, ולא תמצא אותו בשום מקום אחר.

---

## 1. מה זה הפרויקט

CRM בעברית (RTL) לעסק של גיא זורי — תוכנית שחרור חסמים בת 6 שבועות. Next.js 16
עם Turbopack, Tailwind v4, Supabase, פריסה ל-Vercel.

**"פטינה"** היא השפה החזותית: נייר חמים, דיו ירקרק, ורדיגריס כמבטא יחיד. היא
נבנתה קודם כמוקאפ מלא, ואז הועברה לקוד שלב-שלב.

- **המוקאפ:** `patina-crm-mockup.html` בשורש (מקובע ב-git), ומפורסם ב-
  https://claude.ai/code/artifact/598c35d7-afc2-4ddf-93a9-71428204148a
- **התוכנית:** `תכנית עבודה עיצוב.md`

---

## 2. הסטטוס: הכול הושלם

**כל אחד-עשר השלבים בפרודקשן ומאומתים.** 58 קבצים שונו, כ-2,500 שורות נוספו.

| שלב | קומיט |
|---|---|
| 0. יסודות — צבע וגופנים | `fcc9dc6` |
| 1. מסגרת — סרגל צד | `cc72940` |
| 2. רכיבים + מד חלון | `7dfe314` |
| 3א. דף הבית | `9cb7699` |
| 3ב. מי שיצר קשר | `249f3be` |
| 3ג. כרטיס לקוח | `ddab3c1` |
| 3ד. אנשי קשר | `b7240d9` |
| 3ה. פגישות, אירועים, קורסים | `d1e3b95` |
| 3ו. וואטסאפ, ניוזלטר, הגדרות | `81bb0b0` |
| 4. לוח המסע | `079adbb` |
| 5. ליטוש ומצב כהה | `53fca07` |

**שלב אחד = קומיט אחד.** כל אחד ניתן ל-`git revert` בנפרד.

מעבר לעיצוב, באותו מפגש: `32f93ab` (מתג ההשהיה חוסם את הכול) ו-`68cb552`
(אזהרה במסכים שמתזמנים).

### שכבת הרכיבים ב-`globals.css`

מחלקות שנוצרו לאורך העבודה, וכל מסך חדש אמור להשתמש בהן ולא להמציא מחדש:

```
btn-primary/secondary/ghost/danger, card, input, field-label
table-wrap, th, td, tr-hover, cbx, tbl-foot, tag, avc
glyph, pill, sec-label, av, bar, group-row
h-page, page-title, tab, card-h, card-b, card-f
metric, metric-value, day-row, day-now, day-rail, day-now-line
inbox-row, dur-box, date-box, slug, cap-track, fn-row/label/track/value
```

רכיבים משותפים: `avatar.tsx`, `window-meter.tsx`, `metric-tile.tsx`,
`day-timeline.tsx`, `theme-toggle.tsx`, `sending-paused-notice.tsx`.

---

## 3. מלכודות — קרא את זה

### 3.1 RTL: `start` הוא ימין, `end` הוא שמאל

הטעות שחזרה ארבע פעמים בקובץ אחד. ב-`dir="rtl"`:

| Tailwind | בפועל |
|---|---|
| `border-s`, `start-0`, `ms-*`, `ps-*` | **ימין** |
| `border-e`, `end-0`, `me-*`, `pe-*` | **שמאל** |

סרגל בימין המסך מקבל `border-e`. **בדוק כל מחלקה לוגית פעמיים.**

חריג: סימן וי בתיבת סימון צויר בכוונה בצדדים **פיזיים** (`border-t-0
border-r-0`) — וי לא מתהפך בין עברית לאנגלית.

### 3.2 אימות ויזואלי: יש רתמה, אל תבקש מגיא

כל 30 המסכים מאחורי התחברות, וקלוד לא מזין סיסמאות. **זה לא אומר שאי אפשר
לבדוק** — גיא ביקש במפורש שנבדוק בעצמנו.

הפתרון: רתמה שמרנדרת את **הרכיבים האמיתיים** עם ה-**CSS האמיתי** שהשרת
מגיש, לעמוד עצמאי. זה לא הדמיה — זה הפלט האמיתי.

המכניקה (הרתמה בתיקייה זמנית, לא בפרויקט):
1. `ln -s <repo>/node_modules <harness>/node_modules` — אחרת react לא נפתר
2. tsconfig משלה: `baseUrl` לשורש, `paths` ל-`@/*`, stubs ל-`next/link`
   ול-`server-only`, ו-**`include` שמכסה את `src/**/*` של הפרויקט** — בלעדיו
   tsx מקמפל בטרנספורם JSX ישן ומקבלים "React is not defined"
3. `npx tsx@4 --tsconfig ./tsconfig.json render.tsx app.css out.html`
4. הדפדפן הפנימי **לא טוען `file://`** — להגיש ב-`python3 -m http.server`
5. למדוד ב-`javascript_tool` ולא בצילום: הפאנל לעיתים מוסתר, והמדידה
   מדויקת יותר ממילא

מה שהיא תפסה בפועל: חשד לצפיפות שהתברר כשגוי, יחסי ניגודיות שנכשלו, ופסים
שלא היו ביחס הנכון.

**שתי מלכודות מדידה:**
- `getComputedStyle` באמצע transition מחזיר את הערך **הישן**. להמתין.
- `color(srgb r g b)` מחזיר 0–1 ולא 0–255. מחשבון ניגודיות שמחלק ב-255
  ייתן תוצאה שגויה לגמרי — זה קרה, ונראה כמו רגרסיה שלא הייתה.

**אל תריץ סקריפט שמפעיל מנועי שליחה כשההשהיה כבויה.** זה שולח הודעות
אמיתיות. המערכת חסמה את זה בצדק פעם אחת.

### 3.3 מצב כהה — **דלוק עכשיו**

השתנה בשלב 5. `dark:` עדיין מקשיב ל-`data-theme` ולא ל-`prefers-color-scheme`:

```css
@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));
```

אבל עכשיו `data-theme` **נקבע בפועל** על ידי סקריפט חוסם ב-`<head>`
(`layout.tsx`), והמשתמש בוחר במתג בתחתית הסרגל: בהיר / לפי המערכת / כהה.

**המשמעות לכל קוד חדש:**
- אפס `bg-white`. אפס `bg-emerald-50`, `text-gray-500` וכל מחלקת צבע קשיחה
  של Tailwind — הן לא מגיבות ל-`data-theme`. 89 כאלה מופו לאסימונים בשלב 5.
- הבחירה נקראת ב-`useSyncExternalStore` ולא ב-`useState`+`useEffect`. זו לא
  קפריזה: טעינה ל-state אחרי הציור היא בדיוק מקור ההבהוב, ו-lint אוכף את זה
  (`react-hooks/set-state-in-effect`).

**החריג היחיד שנשאר:** `google-meet-badge.tsx` מחזיק `bg-white` בכוונה. הוא
משמש **רק** ב-`/book/[slug]`, שהוא דף ציבורי מחוץ לתוכנית, והלבן שם הוא
מיתוג גוגל. מי שסורק `bg-white` — זו הסיבה שהוא לא הוחלף.

### 3.4 שלושה אסימונים שנולדו מבעיות אמיתיות

- **`--on-primary`** — המוקאפ צובע בועה יוצאת בלבן על ורדיגריס. נכון בבהיר;
  בכהה המבטא הופך לטורקיז בהיר ולבן עליו נותן פחות מ-2:1. האסימון מחזיק לבן
  בבהיר ודיו כהה בכהה. `btn-primary` משתמש בו גם.
- **`--knob`** — ידית מתג, לבנה בשני המצבים **בכוונה**. אסימון ולא ערך קשיח,
  כדי שהכוונה תהיה כתובה ולא תיקרא כ-`bg-white` שנשכח.
- **`--tint-lift`** — 0% בבהיר, 26% בכהה. צבע תוכן (צ׳אקרה, גוון סטטוס)
  שמשמש כטקסט נשאר בדיוק כפי שנבחר בנייר בהיר, ומעורבב אל צבע הדיו רק בכהה,
  שם הוא נופל מתחת לסף.

### 3.5 `status-colors.ts` — שנים-עשר שמות נעולים

המחרוזות חייבות להישאר ליטרליות (Tailwind v4 סורק את המקור), ואותם 12 שמות
אכופים ב-`check constraint` במסד (`0003_statuses.sql`). **משנים מראה, לא
שמות.**

נוסף שם `STATUS_TOKENS` + `statusToken()`: מיפוי 12 השמות לשישה אסימוני
פטינה, למקומות שצריכים **ערך צבע** ולא מחלקה — נקודה על ציר זמן, ריבוע
אייקון. המצב הכהה מגיע איתו בחינם.

### 3.6 מתג ההשהיה חוסם עכשיו את **הכול**

היה חור: המתג הבטיח "השליחה האוטומטית מושהית" אבל חסם וואטסאפ בלבד. שלושה
מנועים עקפו אותו, ובזמן הבדיקה **יצא ניוזלטר בפועל** בעוד `paused` היה true.

תוקן ב-`32f93ab`. נקודת האמת היחידה: `isSendingPaused()` ב-
`src/lib/whatsapp-throttle.ts`.

חלוקת הסיבות ב-`journey-engine`, ששווה להבין לפני שנוגעים:
- `paused` — חלה על **כל** ערוץ. זו כל המשמעות של המתג.
- `time_budget` — חלה על כל ערוץ. הפונקציה עומדת להיקטע, ושליחה שנקטעת לפני
  שנרשמה היא המקור לשליחה כפולה.
- `daily_limit` — **וואטסאפ בלבד.** היא סופרת מה שמטא מחייבת עליו.

`68cb552` הוסיף אזהרה במסכים שמתזמנים (`/newsletter/scheduled`, `/journeys`),
כי אחרת הכשל השקט רק עובר מהמנוע למסך.

### 3.7 כמה שיחות חולקות את אותה תיקייה

זה קרה בפועל: שיחה אחרת קיבעה קומיט **על הענף שלי** באמצע העבודה, ודחפה
ל-`main` בזמן שהכנתי דחיפה משלי.

- **לפני כל דחיפה: `git fetch` ובדיקת הפרש.** הדחיפה נדחתה פעם אחת, והתברר
  ששני קומיטים היו אותו שינוי בדיוק בשני מזהים. `git rebase origin/main`
  השמיט את הכפילות לבד — **לא לכפות דחיפה.**
- **`git add` מפורש לקבצים שלך בלבד.** לא `-A` על השורש. זה הדבר היחיד
  שבאמת מגן — ראה הסעיף הבא.
- **`git status` הוא צילום רגע, לא עובדה יציבה.** שתי שיחות קראו את מצב
  אותם קבצים בהפרש דקות וקיבלו תשובות מנוגדות, כי שיחה שלישית הריצה
  `git add` ביניהן. שתי הקריאות היו נכונות — לזמנן.
  החתימה שמזהה קובץ ב-index באופן חד-משמעי:
  ```
  git diff <file>            → ריק
  git diff --cached <file>   → הדיף המלא
  ```
  מסקנה מעשית: **אל תסמוך על קריאת מצב לפני קומיט.** `git add` עם נתיבים
  מפורשים נכון גם אם המצב השתנה שנייה קודם; קריאת `git status` לא.
- **תיאום עובד.** אפשר לשלוח הודעה לשיחה אחרת דרך
  `mcp__ccd_session_mgmt__send_message` (לא `SendMessage` — הוא לא זמין).
  `list_sessions` נותן את המזהים.
- `npm run build` נוגע ב-`.next/` המשותף. שווה להודיע.

### 3.8 דחיפה בלי להפריע לשיחה השנייה

`git checkout main && git merge` מחזיר קבצים לישן ואז מחיל מחדש — שתי
קומפילציות מיותרות בשרת של השיחה השנייה. במקום זה:

```bash
git branch -f main <ענף>   # מקדם את main בלי לגעת בקבצים
git checkout main          # עץ הקבצים כבר זהה — אפס כתיבות
git push origin main
```

אמת עם `git rev-parse HEAD^{tree}` לפני ואחרי — אותה טביעת אצבע.
(אם כבר עומדים על `main`, `git branch -f` ייכשל. זה תקין, פשוט לדחוף.)

### 3.9 אימות פרודקשן

הדומיין הציבורי הוא `crm-system-eight-omega.vercel.app` בלבד; האחרים חסומים
ב-Vercel SSO ומחזירים 302 על כל בקשה, כך שבדיקה מולם תמיד "עוברת".

ה-CSS יושב ב-`/_next/static/immutable/chunks/*.css` (Turbopack) — **לא**
ב-`/_next/static/css/`. המזעור מוריד מרכאות: חפש `data-theme=dark`.

**האם קומיט מסוים חי:** ה-API של GitHub מחזיר את סטטוס Vercel —
`/repos/<owner>/<repo>/commits/<sha>/status`.

**Tailwind מפצל כלל אחד לכמה בלוקים.** `re.search` על `.btn-primary {...}`
תופס רק את הראשון ונראה כאילו הצהרות נעלמו. `findall`.

### 3.10 לוח המסע — עוצב מחדש, המנוע לא נגע

`journeys/[id]/canvas.tsx` הוא לוח עובד: כרטיסיות נגררות עם שמירת מיקום
לשרת, חיבורים, ותנאי על כל קשת (`journey_edges`, מיגרציה `0019`).

שלב 4 עיצב מחדש: רקע נקודות, חצים בעקומת בזייה שמתחברים לנמלים שעל שפת
הכרטיס, כרטיסייה בשלוש שורות, סידור אוטומטי, ו-× לניתוק בריחוף. **אפס
נגיעה במנוע האוטומציה.**

**מיגרציה אחת כן נדרשה בסוף: `0031_journey_entry_pos.sql`.** לצומת הכניסה
אין שורה ב-`journey_steps`, ולכן לא היה איפה לשמור את מיקומו כשגוררים
אותו. הפתרון הראשון היה localStorage, אבל אז המיקום היה של הדפדפן ולא של
המסע. שתי עמודות על `journeys`, ולא דחיפה ל-`entry_value` שכבר קיים שם —
הוא jsonb עם משמעות שמנוע האוטומציה קורא, וערבוב נתון תצוגה בנתון התנהגות
מזמין את היום שבו שינוי בעיצוב שובר טריגר.

**כיוון הלוח:** הכניסה מעוגנת לקצה הימני (`CANVAS_W` קבוע) והשלבים מתרחקים
שמאלה — כיוון הקריאה בעברית. בלוח שרוחבו נגזר מהתוכן "ימין" זז עם כל
כרטיסייה שנוספת, ולכן הרוחב חייב להיות ידוע מראש.

**שתי מלכודות שעלו כאן ושוות זכירה:**
- `minWidth` על מיכל גלילה מרחיב את **המיכל**, לא את תוכנו. מיכל שגולל
  ומשטח פנימי ברוחב הקבוע הם שני אלמנטים, לא אחד.
- מיכל `dir="ltr"` נפתח משמאל. אם התוכן מתחיל מימין, צריך לגלול לשם
  במפורש — התנהגות הגלילה ב-RTL אינה עקבית בין דפדפנים.

החיבור נשאר **בשתי לחיצות** ולא בגרירה, בכוונה — יש בקוד הערה שמסבירה
שגרירה כמעט בלתי אפשרית במסך מגע. תמיכה בשתיהן היא תוספת התנהגות, לא עיצוב.

### 3.11 שגיאות Server Action נבלעות בפרודקשן

`throw` בעברית מתורגם אצל המשתמש ל-React #441. הדפוס שמחליף אותו קיים —
חפש `ActionForm` ו-`ReplyResult`.

### 3.12 `BUILD-PLAN.md` אומר "אל תמציא מערכת עיצוב חדשה"

הכלל **הוחלף** לצורך עבודת העיצוב. מסוף שלב 2 הוא מצביע על המערכת החדשה,
וכל פיצ׳ר עתידי חייב להשתמש בה.

---

## 4. מה שהיה פתוח — ונסגר

שלושת הפריטים שהמסמך הקודם ציין כפתוחים **כולם נסגרו**, ושניים מהם היו
כבר סגורים והמסמך פשוט לא עודכן:

- **מיגרציה `0030`** — רצה. `webhook_inbox` ו-`meta_form_targets` קיימות
  במסד עם כל העמודות.
- **Postmark** — מאושר ופעיל (`DeliveryType: Live`). ניוזלטר יצא בפועל
  ב-5.9 לנמען אמיתי, עם מסירה מאושרת מגוגל.
- **השליחה האוטומטית** — **הודלקה** ב-5.9 לבקשת גיא, אחרי אימות שהתור ריק
  לגמרי. אפס הודעות יצאו כתוצאה מההדלקה.

**לקח:** שלושתם הופיעו כ"פתוחים" במסמך רק כי הוא לא עודכן. **בדוק מול
המערכת לפני שאתה מדווח מצב** — פעמיים אמרתי לגיא שמשהו חסום כשהוא כבר לא היה.

### מה שלא ידוע

`META_LEADS_VERIFY_TOKEN`, `GROW_WEBHOOK_SECRET` ו-`META_LEADS_PAGE_TOKEN`
קיימים ב-`.env.local`. **האם הם מוגדרים גם ב-Vercel — אי אפשר לדעת מבחוץ:**
נקודת האימות של מטא מחזירה 403 גם על טוקן שגוי וגם על טוקן חסר. רק גיא
יכול לראות, ב-Settings ← Environment Variables.

---

## 5. מה שלא נעשה — במכוון

- **שבבי סינון וחיפוש** ב"מי שיצר קשר", ו**לשוניות** בתיבת התשובה.
  התנהגות חדשה; התיבה מתחלפת אוטומטית לפי מצב החלון וזו ההתנהגות הנכונה.
- **"הערה מהירה", "שליחת הודעה", "קביעת פגישה", "הכנסה למסע"** — אין להם
  פעולה במערכת. כפתור מת גרוע מכפתור חסר.
- **ארבע-עשרה כותרות** נשארו עם `page-title` בלבד ולא הומרו ל-`h-page`.
  הטיפוגרפיה בהן כבר נכונה והמבנה סביב כל אחת שונה.
- **תשעת הדפים הציבוריים** (`/book`, `/event`, `/course`, `/login`, עמודי
  תודה והטמעה) — מחוץ לתוכנית מלכתחילה. הם קיבלו צבעים וגופנים משלב 0 אבל
  לא פריסה. **הלקוחות רואים אותם, לא גיא** — ואפשר לטעון שדף ההרשמה לסדנה
  חשוב יותר ממסך פנימי. השאלה הועלתה פעמיים ולא נענתה.

---

## 6. כללי עבודה שהוסכמו

- **שלב אחד = קומיט אחד**, על ענף נפרד, וניתן לביטול בנפרד.
- **בדיקות סוף שלב:** `npx tsc --noEmit` נקי, `npm run lint` נקי,
  `next build` ללא אזהרות, ואימות ברתמה (3.2).
- **לא נוגעים** ב-`src/app/login`, `src/app/book`, `src/app/api/`, `scripts/`,
  ו**לא ב-`proxy.ts`** — רשימת הנתיבים הציבוריים היא גבול אבטחה, לא נוחות.
- **לא משנים** נתיבי URL, שמות טבלאות או מבנה תיקיות.
- **כל טקסט בממשק בעברית, לשון זכר, RTL.**
- גיא הוא בעל העסק ולא מפתח: **מסך אחד בכל פעם**, הסבר בעברית פשוטה, ובלי
  לבקש ממנו להריץ פקודות. **ובלי לבקש ממנו לבדוק במקומך** — יש רתמה.
